# Sportscene App (Expo + EAS)


A clean Expo Router app with auth + members list.


## Quick Start
1. Install deps:
```bash
npx expo install expo-router @react-native-async-storage/async-storage expo-secure-store
npm i axios